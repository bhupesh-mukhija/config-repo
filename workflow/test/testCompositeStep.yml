# action.yml
name: 'Perform Action'
description: 'Create docker container and run requested operation'
outputs:
  output_var:
    description: 'pacakage version id from package creation job'
runs:
  using: 'composite'
  id: output_test
  run: echo "::set-output name=output_var::$(echo test 'output value')"
  shell: bash